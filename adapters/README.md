# Adapters

Optional host-specific glue. Core SAC is host-agnostic (skills + Python scripts).

| Adapter | Purpose |
|---------|---------|
| `github/` | Future: clone + scan GitHub repos via API |

Deterministic work stays in `scripts/sac_*.py`.
